# dataset-quality-auditor
Dataset Quality Auditor (DQA) is a Python-based auditing tool for tabular machine learning datasets. It detects quality and risk issues such as missing values, outliers, feature correlation, target leakage, train/test overlap, high-cardinality categoricals and data drift and generates structured JSON and HTML reports with dataset health scoring.
